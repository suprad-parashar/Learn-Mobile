<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.learn.android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.SET_ALARM" />

    <application
        android:name=".Learn"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.FAQActivity" />
        <activity android:name=".activities.AddResourceActivity" />
        <activity android:name=".activities.learn.ViewCoursesDataActivity" />
        <activity
            android:name=".activities.SettingsActivity"
            android:configChanges="uiMode" />
        <activity android:name=".activities.AboutActivity" />
        <activity android:name=".activities.learn.SyllabusViewActivity" />
        <activity android:name=".activities.learn.CourseVideoViewActivity" />
        <activity android:name=".activities.learn.CourseViewActivity" />
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".activities.AuthActivity"
            android:configChanges="uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.HomeActivity"
            android:label="@string/title_activity_home" />

        <receiver android:name=".utils.NotificationReceiver" />
    </application>

</manifest>